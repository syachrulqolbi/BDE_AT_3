sources:
  - name: raw
    description: Raw data from CSV files
    database: postgres
    schema: bronze

    tables:
      - name: raw_census_g01
      - name: raw_census_g02
      - name: raw_lga
      - name: raw_listing
      - name: raw_suburb